// Art direction only. This is intentionally not a visitor-facing control.
export const config = {
  initialSelection: "self",
  fieldDensity: 1.0, // 0.5–1.6; changing this never moves content.
  tagline: "", // Optional proposed copy: 'Thinking in structures.'
  social: { github: "", linkedin: "" },
};
